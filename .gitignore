# Python
__pycache__/
*.py[cod]
*.pyo
.env

# Large data files
data/intel/archive/
data/intel/random_search_full/trial_*.json
data/intel/random_search_full/trial_*_sim.json
Fantasy-Premier-League/

# Models (large binaries)
models/*.pkl

# Stage 10 in-season fine-tune checkpoints — experimental, OFF by default
# (step 6 measured they hurt holdout MAE); regenerate with stage10_finetune.py
models/stage10/ft_*.npz

# Locks
*.lock

# OS
.DS_Store
Thumbs.db

# vaastav repo downloads (re-fetchable via pipeline/build_season_inputs.py)
data/raw/vaastav_repo/
